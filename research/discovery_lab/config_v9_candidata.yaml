# ============================================================================
# CONFIG v9 CANDIDATA — pós-correção do laboratório (2026-07-04)
# NÃO está em produção. Esta é a fotografia dos overrides que embasaram a
# integração v9 (o config de produção contém as chaves finais F16-F20).
#
# Evidência pós-correção (teto de alavancagem 3x + cobertura 30d):
#   medianas +54.54 / +367.66 / +335.59 nos 3 cortes válidos,
#   hit 1.0 / 1.0 / 0.5, 10 aprovados na seleção atual.
# Observação: dois critérios (hit do corte2 e nº de aprovados) ficam no limite
# da amostra; por isso a integração v9 será PR draft + recomendação de sombra.
# ============================================================================
entry_rule:
  min_positive_windows: 0
  required_windows: []
hard_filters:
  f1_recent_activity_days: 21
  f2_min_closed_trades: 15
  f2b_min_trades_30d: 3
  f5_max_drawdown_90d_pct: 80.0
  f16_min_coverage_days: 30
  f17_min_sim_net_usd: 10.0
  f18_sim_positive_halves: true
  f19_max_sim_dd_pct: 25.0
  f20_max_trader_equity_usd: 150000
copy_simulation:
  window_days: 60
  latency_slippage_pct: 0.03
  max_copy_leverage: 3.0
score_adjustments:
  min_score_for_suggestion: 0.0
